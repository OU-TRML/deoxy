language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
    - rust: beta
script:
  - cargo build --features "stub" --verbose
  - cargo test --features "stub" --verbose